# Merge to Main
1. List all open PRs with `gh pr list`
2. For each PR, check if it's mergeable
3. Merge each PR, resolving conflicts if needed
4. Push main to origin
5. Summarize what was merged

IMPORTANT: Always confirm the current branch and worktree path before any git operations.
